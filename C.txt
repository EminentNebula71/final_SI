En la primera consulta se busca en una query interna  para después
usar el select customerid.

En la segunda consulta se usa el union all para juntar ambas subqueries,
y finalmente buca las que tengan el count(*)=1. Es la que menos tarda en
empezar a imprimir.

En la última consulta hace dos queries y después usa el except.